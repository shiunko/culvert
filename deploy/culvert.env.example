CULVERT_CONFIG_DIR=/etc/culvert
CULVERT_DATA_DIR=/var/lib/culvert/data

# app-image 已内置通用 JVM 参数；此处只指定依赖生产目录的诊断参数。
# 仅在完成容量与敏感数据评估后启用 heap dump；不要在此文件中存放秘密。
JAVA_TOOL_OPTIONS=-XX:ErrorFile=/var/lib/culvert/diagnostics/hs_err_pid%p.log -XX:HeapDumpPath=/var/lib/culvert/diagnostics
